# requirements.txt — 多因子策略 v2.0 完整依赖清单
# 来源: docs/tech-plan.md §九

# 核心数据
tushare>=1.4.26
baostock>=0.8.0            # [v2.0] 免费备选数据源
polars>=1.0.0
duckdb>=1.1.0
pyarrow>=15.0.0

# 配置与日志
structlog>=24.0.0

# 数值计算
numpy>=1.26.0
scipy>=1.13.0

# [v2.0] 统计检验
# pypbo>=0.4.0               # PBO (Probability of Backtest Overfitting)
# ↑ pypbo 不在 PyPI 上 — Phase 3 时需寻找替代方案或自行实现 PBO 计算

# 可视化与报告
matplotlib>=3.8.0
seaborn>=0.13.0
tabulate>=0.9.0

# 开发工具
python-dotenv>=1.0.0
ipykernel>=6.29.0
jupyter>=1.0.0

# 测试
pytest>=8.0.0
pytest-cov>=5.0.0
